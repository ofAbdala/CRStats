# Cole na Vercel (Project Settings > Environment Variables)
SUPERCELL_TOKEN=REPLACE_WITH_YOUR_TOKEN
# Opcional: usar proxy RoyaleAPI quando IP variar
USE_PROXY=false
# Tag padrão carregada (pode trocar no site)
DEFAULT_TAG=U9UUCCQ