/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: ['@crstatus/shared'],
    images: {
        domains: ['tjfzplxjsffddlvwtdtq.supabase.co'],
    },
};

export default nextConfig;
